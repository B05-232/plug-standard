name: C/C++ CI

on:
  push:
    branches:
    - "master"
    - "*-draft"
  pull_request:
    branches:
    - "master"
    - *-draft"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
    - name: make check
      run: make check
